services:
  solar-charger:
    build: .
    container_name: solar-charger
    restart: always
    network_mode: host
    volumes:
      - ./config.json:/app/config.json
      - ./logs:/app/logs
    environment:
      - TZ=Europe/Berlin
